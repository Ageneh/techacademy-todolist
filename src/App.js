import React from 'react'
import TodoContainer from "./Pages/TodoContainer"

export const App = (props) => {
    return (
        <>
            <TodoContainer/>
        </>
    );
};

export default App
